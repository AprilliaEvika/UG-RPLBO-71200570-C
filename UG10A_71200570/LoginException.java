package com.ug10;

public class LoginException extends com.ug10.User {
    private String username;
    private String password;

    public String username{
        if (username == null) {
            System.out.println("Username tidak boleh kosong");
        }

        else if (username == (String username));

        else if (username != (String username)){
            System.out.println("Username anda tidak sesuai ");
        }
    }

    public String password{
        if (password == null) {
            System.out.println("Password tidak boleh kosong");
        }

        else if (password == (String password));

        else if (password != (String password)){
            System.out.println("Password anda tidak sesuai");
        }
    }

}
